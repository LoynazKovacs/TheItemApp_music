# HeartMuLa music foundation models (Apache-2.0). Pulls its own pinned torch
# stack (torchtune, torchao, transformers, bitsandbytes, vector-quantize-pytorch,
# accelerate, soundfile, ...) — all compatible with the base image's torch 2.4.1.
heartlib @ git+https://github.com/HeartMuLa/heartlib.git

# HTTP server.
fastapi==0.115.6
uvicorn[standard]==0.34.0

# Checkpoint downloads from the HuggingFace Hub. Leave the range open within the
# band transformers (pulled by heartlib) requires, so pip can co-resolve them.
huggingface_hub>=0.34.0,<1.0
